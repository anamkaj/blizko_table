import { getClient } from "@/src/shared/api/list-client-blizko/get-client";
import { getPay } from "@/src/shared/api/list-client-blizko/get-pay";
import LineClient from "@/src/widgets/line-client";

export default async function MainPage() {
 

  return (
    <>
      {/* <LineClient clientList={clientList} payList={payList} /> */}
    </>
  );
}
